language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install python-coveralls
  - pip install coverage
  - pip install -r requirements.txt
# # command to run tests
script: nosetests --nocapture --with-coverage --cover-erase --cover-package=lib
after_success:
  - coveralls
